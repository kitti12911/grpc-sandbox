name: Release

on:
    workflow_call:
        inputs:
            release_branch:
                required: true
                type: string
            release_sha:
                required: true
                type: string
        outputs:
            published:
                value: ${{ jobs.version.outputs.published }}
            version:
                value: ${{ jobs.version.outputs.version }}
            git_tag:
                value: ${{ jobs.version.outputs.git_tag }}
            image_digest:
                value: ${{ jobs.publish.outputs.image_digest }}
            arm64_digest:
                value: ${{ jobs.publish.outputs.arm64_digest }}

concurrency:
    group: ${{ github.workflow }}-publish
    cancel-in-progress: false

permissions:
    contents: read

env:
    IMAGE_NAME: grpc-sandbox
    RELEASE_BRANCH: ${{ inputs.release_branch }}
    RELEASE_SHA: ${{ inputs.release_sha }}

jobs:
    version:
        name: Publish Semantic Release
        runs-on: ubuntu-latest
        container:
            image: ${{ vars.TOOLCHAIN_REGISTRY }}/${{ vars.TOOLCHAIN_IMAGE_NAMESPACE }}/release-toolchain@sha256:aaba512b7e0bce0b866ce97ff6eb3cb27b7a58ba4cb4995c9fa64af571850193
            credentials:
                username: ${{ secrets.TOOLCHAIN_REGISTRY_USERNAME }}
                password: ${{ secrets.TOOLCHAIN_REGISTRY_PASSWORD }}
        permissions:
            contents: write
            issues: write
            pull-requests: write
        outputs:
            published: ${{ steps.semantic.outputs.published }}
            version: ${{ steps.semantic.outputs.version }}
            git_tag: ${{ steps.semantic.outputs.git_tag }}
            env_tag: ${{ steps.semantic.outputs.env_tag }}
        steps:
            # actions/checkout v6.0.2
            - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
              with:
                  ref: ${{ env.RELEASE_BRANCH }}
                  fetch-depth: 0

            - name: Pin release commit
              run: |
                  git config --global --add safe.directory "$(pwd)"
                  git reset --hard "${RELEASE_SHA}"

            - id: semantic
              name: Publish semantic release
              run: CI_OUTPUT_FILE="${GITHUB_OUTPUT}" ./scripts/ci/semantic-release-publish.sh --no-ci
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    build:
        name: Build and Scan Image (${{ matrix.arch }})
        runs-on: ${{ matrix.runner }}
        needs:
            - version
        if: needs.version.outputs.published == 'true'
        strategy:
            fail-fast: false
            matrix:
                include:
                    # Re-enable when x86 images are needed again.
                    # - arch: amd64
                    #   platform: linux/amd64
                    #   runner: ubuntu-latest
                    - arch: arm64
                      platform: linux/arm64
                      runner: ubuntu-24.04-arm
        env:
            REGISTRY: ${{ vars.IMAGE_REGISTRY }}
            IMAGE_NAMESPACE: ${{ vars.IMAGE_NAMESPACE }}
            IMAGE_REF: ${{ vars.IMAGE_REGISTRY }}/${{ vars.IMAGE_NAMESPACE }}/grpc-sandbox
            STAGING_IMAGE_REF: ${{ vars.IMAGE_REGISTRY }}/${{ vars.IMAGE_NAMESPACE }}/grpc-sandbox:staging-${{ inputs.release_sha }}-${{ matrix.arch }}
            ARCH_IMAGE_REF: ${{ vars.IMAGE_REGISTRY }}/${{ vars.IMAGE_NAMESPACE }}/grpc-sandbox:${{ needs.version.outputs.git_tag }}-${{ matrix.arch }}
            SHA_ARCH_IMAGE_REF: ${{ vars.IMAGE_REGISTRY }}/${{ vars.IMAGE_NAMESPACE }}/grpc-sandbox:${{ inputs.release_sha }}-${{ matrix.arch }}
            TOOLCHAIN_IMAGE: ${{ vars.TOOLCHAIN_REGISTRY }}/${{ vars.TOOLCHAIN_IMAGE_NAMESPACE }}/image-toolchain@sha256:55e03f1a59c20fbd37a400a95924be84fbd1814db2bf511c4fdb3fdefbcf6b2c
        steps:
            # actions/checkout v6.0.2
            - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
              with:
                  ref: ${{ env.RELEASE_SHA }}

            - name: Check release inputs
              run: ./scripts/ci/require-env.sh REGISTRY IMAGE_NAMESPACE IMAGE_REF STAGING_IMAGE_REF ARCH_IMAGE_REF SHA_ARCH_IMAGE_REF TOOLCHAIN_IMAGE

            - name: Set up Docker Buildx
              run: ./scripts/ci/setup-buildx.sh

            - name: Login to image registry
              run: ./scripts/ci/login-registry.sh
              env:
                  REGISTRY_USERNAME: ${{ secrets.IMAGE_REGISTRY_USERNAME }}
                  REGISTRY_PASSWORD: ${{ secrets.IMAGE_REGISTRY_PASSWORD }}

            - name: Build and push staging image
              run: ./scripts/ci/build-image.sh
              env:
                  IMAGE_CONTEXT: .
                  IMAGE_PLATFORM: ${{ matrix.platform }}
                  IMAGE_TAG: ${{ env.STAGING_IMAGE_REF }}
                  BUILD_CACHE_FROM: type=gha,scope=grpc-sandbox-${{ matrix.arch }}
                  BUILD_CACHE_TO: type=gha,mode=max,scope=grpc-sandbox-${{ matrix.arch }}
                  BUILD_OUTPUT: type=image,push=true,oci-mediatypes=true
                  BUILD_PROVENANCE: true
                  BUILD_SBOM: true

            - name: Scan image with Trivy
              run: ./scripts/ci/scan-image.sh
              env:
                  TRIVY_IMAGE_REF: ${{ env.STAGING_IMAGE_REF }}
                  TRIVY_REPORT: ${{ runner.temp }}/trivy-results.json
                  TRIVY_QUIET: true
                  TRIVY_IMAGE_SRC: remote
                  TRIVY_PLATFORM: ${{ matrix.platform }}
                  TRIVY_USERNAME: ${{ secrets.IMAGE_REGISTRY_USERNAME }}
                  TRIVY_PASSWORD: ${{ secrets.IMAGE_REGISTRY_PASSWORD }}

            - name: Promote scanned image tags
              run: ./scripts/ci/promote-image.sh

    publish:
        name: Publish Multi-Arch Image
        runs-on: ubuntu-latest
        needs:
            - version
            - build
        if: needs.version.outputs.published == 'true'
        outputs:
            image_digest: ${{ steps.image.outputs.digest }}
            arm64_digest: ${{ steps.image.outputs.arm64_digest }}
        env:
            REGISTRY: ${{ vars.IMAGE_REGISTRY }}
            IMAGE_NAMESPACE: ${{ vars.IMAGE_NAMESPACE }}
            IMAGE_REF: ${{ vars.IMAGE_REGISTRY }}/${{ vars.IMAGE_NAMESPACE }}/grpc-sandbox
            RELEASE_TAG: ${{ needs.version.outputs.git_tag }}
            ENV_TAG: ${{ needs.version.outputs.env_tag }}
            IMAGE_ARCHES: arm64
        steps:
            # actions/checkout v6.0.2
            - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
              with:
                  ref: ${{ env.RELEASE_SHA }}

            - name: Check release inputs
              run: ./scripts/ci/require-env.sh REGISTRY IMAGE_NAMESPACE IMAGE_REF RELEASE_TAG

            - name: Set up Docker Buildx
              run: ./scripts/ci/setup-buildx.sh

            - name: Login to image registry
              run: ./scripts/ci/login-registry.sh
              env:
                  REGISTRY_USERNAME: ${{ secrets.IMAGE_REGISTRY_USERNAME }}
                  REGISTRY_PASSWORD: ${{ secrets.IMAGE_REGISTRY_PASSWORD }}

            # sigstore/cosign-installer v4.1.1
            - uses: sigstore/cosign-installer@cad07c2e89fa2edd6e2d7bab4c1aa38e53f76003

            - name: Create multi-arch manifest
              run: ./scripts/ci/publish-manifest.sh

            - id: image
              name: Resolve image digests
              run: CI_OUTPUT_FILE="${GITHUB_OUTPUT}" ./scripts/ci/resolve-image-digests.sh

            - name: Sign Docker images
              run: ./scripts/ci/sign-image.sh
              env:
                  COSIGN_PRIVATE_KEY: ${{ secrets.COSIGN_PRIVATE_KEY }}
